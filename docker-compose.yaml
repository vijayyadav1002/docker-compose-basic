version: "3.3"
services:
  web:
    container_name: centos7-php8.1
    working_dir: /var/www/html
    build:
        context: .
    network_mode: bridge
    volumes:
      - ./app:/var/www/html
#    command: >
#      bash -c "
#          tail -f /dev/null
#        "
    ports:
      - "80:80"
